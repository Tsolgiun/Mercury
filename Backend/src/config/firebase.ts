// This file is kept for backward compatibility but no longer contains Firebase configuration
// Firebase authentication has been removed in favor of JWT-based authentication

console.log('Firebase integration has been removed. Using JWT authentication only.');

// Export null values to indicate services are not available
export const auth = null;
export const firestore = null;
export const storage = null;

export default null;
